/**
 * English copy for Admin Hub.
 *
 * Admin Hub is English-only: there is no locale switcher, cookie, or Spanish
 * dictionary. `t()` interpolates `{token}` placeholders from the EN catalog.
 */
export { ADMIN_HUB_DATE_LOCALE, t, formatAdminHubPeriod, formatRelativeTime } from "./translate";
export type { AdminHubTranslate, AdminHubTranslateParams } from "./translate";
